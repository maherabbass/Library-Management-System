# Secrets — never in Docker context
.env
.env.*
!.env.example

# Python caches
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# Git
.git/
.gitignore

# CI / docs (not needed in image)
.github/
CLAUDE.md
README.md

# Local dev only
docker-compose.yml
